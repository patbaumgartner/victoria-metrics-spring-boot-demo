<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath></relativePath>
  </parent>

  <groupId>com.example</groupId>
  <artifactId>victoria-metrics-demo</artifactId>
  <version>1.0.0</version>
  <name>VictoriaMetrics Spring Boot Demo</name>
  <description>Spring Boot demo application with OpenTelemetry integration</description>

  <properties>
    <java.version>25</java.version>
    <opentelemetry-logback-appender.version>2.30.0-alpha</opentelemetry-logback-appender.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!--
        Spring Boot manages the stable OpenTelemetry artifacts via `opentelemetry-bom`
        (${opentelemetry.version}), but NOT the `-alpha` ones. The Logback appender pulls in
        `opentelemetry-api-incubator` transitively, which would otherwise resolve to a newer
        version than the Boot-managed SDK and fail at runtime with NoClassDefFoundError.
        Importing the alpha BOM at the exact same version keeps stable and incubating
        artifacts on one consistent OpenTelemetry release.
      -->
      <dependency>
        <groupId>io.opentelemetry</groupId>
        <artifactId>opentelemetry-bom-alpha</artifactId>
        <version>${opentelemetry.version}-alpha</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <!--
      Brings the whole telemetry stack in one starter: the Micrometer metrics starter,
      the OTLP meter registry, the Micrometer Tracing OpenTelemetry bridge and the OTLP
      exporter. Declaring any of those separately would only duplicate this starter.
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-opentelemetry</artifactId>
    </dependency>

    <!-- AspectJ enables @Observed (Micrometer Observation) annotation support -->
    <dependency>
      <groupId>org.aspectj</groupId>
      <artifactId>aspectjweaver</artifactId>
    </dependency>

    <!--
      Bridges Logback logs into OpenTelemetry for OTLP log export. Not part of Spring
      Boot, so it is versioned here and kept in sync with the SDK by the alpha BOM above.
    -->
    <dependency>
      <groupId>io.opentelemetry.instrumentation</groupId>
      <artifactId>opentelemetry-logback-appender-1.0</artifactId>
      <version>${opentelemetry-logback-appender.version}</version>
    </dependency>

    <!-- Testing: the OpenTelemetry test starter also provides spring-boot-starter-test -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-opentelemetry-test</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- In-memory span/log exporters so tests can assert on real exported telemetry -->
    <dependency>
      <groupId>io.opentelemetry</groupId>
      <artifactId>opentelemetry-sdk-testing</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

</project>
